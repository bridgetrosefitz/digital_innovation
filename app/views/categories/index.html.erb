<h1>Innovation categories</h1>

<% @categories.each do |category| %>
<li><%= link_to category.name, category_path(category) %></li>
    <ul>
    <% category.indicators.each do |indicator| %>
    <li><%= indicator.name %></li>    
    <% end %>
    </ul>
<% end %>